#include <bits/stdc++.h>
using namespace std;

void solve(int n) {
    int arr[n];
    int count=0;
    for (int i = 0; i < n; i++) {
        cin >> arr[i];  
    }
}
  


int main() {
    int t; // number of test cases
    cin >> t;

    while (t--) {
        int n; // size of the array
        cin >> n;
        solve(n);
    }
    return 0;
}